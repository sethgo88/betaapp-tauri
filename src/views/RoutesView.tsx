const RoutesView = () => {
	return (
		<div className="flex flex-col items-center justify-center pt-24 gap-2">
			<p className="text-stone-400">Routes — coming in Phase 9.</p>
		</div>
	);
};

export default RoutesView;
